import React, { Component } from "react";
import PropTypes from "prop-types";

/**
 * The component
 * @type {Object}
 */
class Reproducibility extends Component {
  /**
   * Render
   * @return {ReactElement}
   */
  render() {
    return <div> Reproducibility </div>;
  }
}
Reproducibility.propTypes = {};
export default Reproducibility;
